java的可变长参数：
	类型... 参数名
	1、可变长参数传递的实参时0~n个
	2、如果有精确匹配的方法，则调用该方法，不会执行可变长参数方法
	3、可变长参数可以等同看为数组，调用方法可以直接传数组
	4、可变长参数只能放在形式参数列表的最后一个，并且只能有一个可变长参数
	m1(int... i){sout("test");}
	m1(int i){sout(i);}
	main(String [] args){
		m1();//test
		m1(1);//1
		m1(1,2,3,4);//test
		m1(new int[]{1,2,34,4})//test
	}
	