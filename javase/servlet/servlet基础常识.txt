servlet
    javaee核心规范，定义了如何接受和响应请求
    web层拿到request、response什么问题都能解决
    HttpServlet:基于http协议实现类Servlet接口
一、互联网通信
	1、互联网中的通信角色
		客户端
			用于发起请求，索要资源文件内容的计算机，称为客户端计算机 

		服务端
			用于接收请求，提供共享资源文件内容的计算机，称为服务端计算机

		
	2、互联网通信模型
		协议:
			协议头:
			协议体:
				OSI七层参考模型:描述两台计算机如何通信的	计算机能够通信的规范

				TCP/IP参考模型:遵守OSI七层模型
					HTTP协议

				Netty框架
				
				
			发送短息
				UDP协议
		
		网络编程:Socket网络编程
		网页编程：我们学的是
		C/S:TCP/IP
			C:Client
			S:Server
			特征:
				需要安装到终端
				定期更新


		B/S:
			B:Browser 浏览器
			S:Server	服务器
			特征:
				不需要定期更新

	3、共享资源文件指得是可以通过网络进行传输文件。理论上来说硬盘上所有文件都可以通过网络进行传输。所以硬盘上所有看到文件都可以称为【共享资源文件】
	     资源文件分类
		区别:数据是否是动态的
		静态资源文件指的是文件内容相对固定，所有用户来访问时看到的内容都是一样的文件
			指html、css、js、图片、视频
		动态资源文件指的是文件内容可以根据用户请求不同而产生不同的变化
			java类以及通过代码得到的数据

二、浏览器的行为管理
	1、行为控制分类
		1.1	浏览器的请求行为
			请求头:
				Request URL
				Request Method	get/post
				Status Code		200/404/501
				Remote(远程) Address	IP:port
			请求体:
				GET
				200ok:客户端访问服务器成功并且服务器给客户端相应结果

		1.2	浏览器的接收行为


	2、浏览器的请求方式 七种方式，java中只考虑get、post
		get:
			常见的get请求：location，超链接，form表单
			1、请求参数在地址栏中显示
			2、携带的【请求参数数量】不能超过4k
			3、传输速度快，不安全

		post:
			常见post请求:form表单
			1、地址栏看不到请求参数
			2、理论上携带数据的大小不受限制
			3、相对安全
	由于POST方式允许将客户端文件作为请求参数发送到服务端。
	因此有机会将病毒文件发送到服务端计算机进行攻击，因此绝大多数门户级网站拒绝接收POST方式请求，所以在平时开发过程绝大多数情况都使用GET方式
		- 在进行文件上传时，必须使用POST方式。此时可以根据检测上文文件类型来杜绝用户传输病毒文件
	
	- 在进行登录验证操作时，为了防止用户名和密码在浏览器地址上展示，泄露用户信息，因此要选择POST方式

		- 在获取服务端实时变化的数据时候，比如股票价格，火车票情况等，应该选择POST方式
	表单域标签由三类标签组成

- input
- select
- textarea
		在FORM标签命令浏览器发送请求时，浏览器自动的读取FORM标签内部的表单域标签。
		将表单域标签的name属性的值，作为”请求参数名”。将表单域标签的value属性值，作为”请求参数内容”
	表单域标签作为请求参数条件


		- 表单域标签必须声明在FORM标签内部

		- 表单域标签必须声明name属性

		- 由readOnly修饰的表单域标签可以作为请求参数。但是由disabled修饰的表单域标签将失去作为请求参数条件

		- 对于CHECKBOX与RADIO标签，只有在被选中的情况下才可以作为请求参数
三、Tomcat服务器